# AWS credentials are bound to this ServiceAccount via an EKS Pod Identity association
# (RoleArn = LabRole) created by CloudFormation - no IRSA IAM role to create.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: sentinel-scanner
  namespace: sentinel
